{
  "name": "kuuga-template",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm run init",
    "init": "kuuga init",
    "add": "kuuga add",
    "validate": "kuuga validate",
    "build": "kuuga build"
  },
  "dependencies": {
    "kuuga-cli": "^0.12.1"
  }
}
